generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// Bảng dữ liệu tham chiếu - Văn bằng/Lý lịch công tác
model DmDtVanBangLLCT {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_dt_van_bang_llct")
}

/// Bảng dữ liệu tham chiếu - Hình thức kỷ luật
model DmHinhThucKyLuat {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(50)
  ten       String   @db.VarChar(500)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_hinh_thuc_ky_luat")
}

/// Bảng dữ liệu tham chiếu - Loại cán bộ
model DmLoaiCanBo {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_loai_can_bo")
}

/// Bảng dữ liệu tham chiếu - Loại chức vụ
model DmLoaiChucVu {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(70)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_loai_chuc_vu")
}

/// Bảng dữ liệu tham chiếu - Loại phụ cấp
model DmLoaiPhuCap {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_loai_phu_cap")
}

/// Bảng dữ liệu tham chiếu - Ngạch CDNN
model DmNgachCDNN {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(20)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_ngach_cdnn")
}

/// Bảng dữ liệu tham chiếu - Ngân hàng
model DmNganHang {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(20)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_ngan_hang")
}

/// Bảng dữ liệu tham chiếu - Nhóm máu
model DmNhomMau {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_nhom_mau")
}

/// Bảng dữ liệu tham chiếu - Nơi cấp CCCD
model DmNoiCapCCCD {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_noi_cap_cccd")
}

/// Bảng dữ liệu tham chiếu - Quân hàm
model DmQuanHam {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_quan_ham")
}

/// Bảng dữ liệu tham chiếu - Quan hệ gia đình
model DmQuanHeGiaDinh {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_quan_he_gia_dinh")
}

/// Bảng dữ liệu tham chiếu - Quốc gia
model DmQuocGia {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  maChauLuc String?  @db.VarChar(10)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_quoc_gia")
}

/// Bảng dữ liệu tham chiếu - Thành phần xuất thân
model DmThanhPhanXuatThan {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_thanh_phan_xuat_than")
}

/// Bảng dữ liệu tham chiếu - Khen thưởng
model DmKhenThuong {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_khen_thuong")
}

/// Bảng dữ liệu tham chiếu - Phương thức khen thưởng
model DmPhuongThucKhenThuong {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_phuong_thuc_khen_thuong")
}

/// Bảng dữ liệu tham chiếu - Tôn giáo
model DmTonGiao {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_ton_giao")
}

/// Bảng dữ liệu tham chiếu - Trình độ chuyên môn
model DmTrinhDoChuyenMon {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_trinh_do_chuyen_mon")
}

/// Bảng dữ liệu tham chiếu - Trình độ phổ thông
model DmTrinhDoPhoThong {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_trinh_do_pho_thong")
}

/// Bảng dữ liệu tham chiếu - Xếp loại chuyên môn
model DmXepLoaiChuyenMon {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(50)
  ten       String   @db.VarChar(500)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_xep_loai_chuyen_mon")
}

/// Bảng dữ liệu tham chiếu - Nhóm lương
model DmNhomLuong {
  id          Int      @id @default(autoincrement())
  nhomLuong   String   @unique @db.VarChar(10)
  maBacLuong  Int
  tenBacLuong String   @db.VarChar(10)
  heSoLuong   Decimal  @db.Decimal(10, 2)
  createdAt   DateTime @default(now()) @db.Timestamp(6)
  updatedAt   DateTime @updatedAt @db.Timestamp(6)

  @@index([nhomLuong])
  @@map("dm_nhom_luong")
}

/// Bảng dữ liệu tham chiếu - Vị trí việc làm
model DmViTriViecLam {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(20)
  ten       String   @db.VarChar(100)
  loaiVtvl  String?  @db.VarChar(20)
  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  updatedAt DateTime @updatedAt @db.Timestamp(6)

  @@index([ma])
  @@map("dm_vi_tri_viec_lam")
}

model DmGioiTinh {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(20)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_gioi_tinh")
}

model DmChauLuc {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(10)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_chau_luc")
}

model DmChucDanhKhoaHoc {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_chuc_danh_khoa_hoc")
}

model DmChucVu {
  id         Int      @id @default(autoincrement())
  ma         String   @unique @db.VarChar(5)
  ten        String   @db.VarChar(200)
  heSoPhuCap Float?
  prefixText String?  @db.VarChar(50)
  loaiChucVu String?  @db.VarChar(5)
  priority   Int?
  active     Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([ma])
  @@map("dm_chuc_vu")
}

model DmChuyenNganhBGD {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(20)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_chuyen_nganh_bgd")
}

model DmDanToc {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dan_toc")
}

model DmDanhHieuNhaNuoc {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_danh_hieu_nha_nuoc")
}

model DmDoiTuongChinhSach {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_doi_tuong_chinh_sach")
}

model DmDtChungChiBDNV {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_chung_chi_bdnv")
}

model DmDtChungChiNgoaiNgu {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(20)
  ten       String   @db.VarChar(100)
  ngoaiNgu  String?  @db.VarChar(5)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_chung_chi_ngoai_ngu")
}

model DmDtChungChiTinHoc {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(100)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_chung_chi_tin_hoc")
}

model DmDtDoiTuongAnqp {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(200)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_doi_tuong_anqp")
}

model DmDtHinhThucCM {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_hinh_thuc_cm")
}

model DmDtHinhThucLLCT {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_hinh_thuc_llct")
}

model DmDtNgoaiNgu {
  id        Int      @id @default(autoincrement())
  ma        String   @unique @db.VarChar(5)
  ten       String   @db.VarChar(50)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([ma])
  @@map("dm_dt_ngoai_ngu")
}

/// Bảng lý lịch cán bộ
model TcnsLyLich {
  id                Int       @id @default(autoincrement())
  shcc              String    @unique @db.VarChar(255)
  maNhanVien        String    @db.VarChar(20)
  ho                String    @db.VarChar(150)
  ten               String    @db.VarChar(50)
  ngaySinh          DateTime? @db.Timestamp(6)
  thanhPhanGiaDinh  String?   @db.VarChar(5)
  thanhPhanBanThan  String?   @db.VarChar(5)
  gioiTinh          String?   @db.VarChar(5)
  danToc            String?   @db.VarChar(5)
  tonGiao           String?   @db.VarChar(5)
  quocTich          String?   @db.VarChar(5)
  emailCaNhan       String?   @db.VarChar(70)
  chieuCao          Int?
  canNang           Int?
  nhomMau           String?   @db.VarChar(5)
  ngayNhapNgu       DateTime? @db.Timestamp(6)
  ngayXuatNgu       DateTime? @db.Timestamp(6)
  quanHam           String?   @db.VarChar(5)
  ngayVaoDang       DateTime? @db.Timestamp(6)
  ngayVaoDangCt     DateTime? @db.Timestamp(6)
  ngayVaoDoan       DateTime? @db.Timestamp(6)
  maSoThue          String?   @db.VarChar(20)
  stk               String?   @db.VarChar(20)
  nganHang          String?   @db.VarChar(100)
  email             String?   @db.VarChar(70)
  chiNhanh          String?   @db.VarChar(200)
  biDanh            String?   @db.VarChar(100)
  cccd              String?   @db.VarChar(20)
  sucKhoe           String?
  isDangVien        Boolean?  @default(false)
  ngayVaoCongDoan   DateTime? @db.Timestamp(6)
  bhxh              String?   @db.VarChar(20)
  doiTuongChinhSach String?   @db.VarChar(100)
  bhyt              String?   @db.VarChar(20)
  ngayCapCccd       DateTime? @db.Timestamp(6)
  noiCapCccd        String?   @db.VarChar(5)
  sdt               String?   @db.VarChar(20)
  createdAt         DateTime  @default(now()) @db.Timestamp(6)
  updatedAt         DateTime  @updatedAt @db.Timestamp(6)

  @@index([shcc])
  @@index([maNhanVien])
  @@map("tcns_ly_lich")
}

/// Bảng cán bộ
model TcnsCanBo {
  id         Int       @id @default(autoincrement())
  shcc       String?   @unique @db.VarChar(20)
  ho         String?   @db.VarChar(150)
  ten        String?   @db.VarChar(50)
  ngaySinh   DateTime? @db.Timestamp(6)
  gioiTinh   String?   @db.VarChar(5)
  maNhanVien String?   @db.VarChar(20)
  email      String?   @db.VarChar(70)
  sdt        String?   @db.VarChar(20)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@map("tcns_can_bo")
}

/// Bảng Người học (Sinh viên)
model NguoiHoc {
  id                   Int        @id
  cccdSo               String?    @db.VarChar(20)
  cccdNgayCap          DateTime?
  cccdNoiCap           String?    @db.VarChar(255)
  ho                   String?    @db.VarChar(255)
  ten                  String?    @db.VarChar(255)
  ngaySinh             DateTime?
  noiSinhPhuongXa      String?    @db.VarChar(50)
  emailCaNhan          String?    @db.VarChar(255)
  soDienThoai          String?    @db.VarChar(50)
  gioiTinh             String?    @db.VarChar(5)
  quocTich             String?    @db.VarChar(50)
  tonGiao              String?    @db.VarChar(50)
  danToc               String?    @db.VarChar(50)
  chaHoTen             String?    @db.VarChar(255)
  chaNamSinh           Int?       @db.SmallInt
  chaNgheNghiep        String?    @db.VarChar(255)
  chaNoiCongTac        String?    @db.VarChar(50)
  chaDienThoai         String?    @db.VarChar(50)
  meHoTen              String?    @db.VarChar(255)
  meNamSinh            Int?       @db.SmallInt
  meNgheNghiep         String?    @db.VarChar(255)
  meNoiCongTac         String?    @db.VarChar(50)
  meDienThoai          String?    @db.VarChar(50)
  nguoiLienHeHoTen     String?    @db.VarChar(255)
  nguoiLienHePhuongXa  String?    @db.VarChar(50)
  nguoiLienHeSoNha     String?    @db.VarChar(255)
  nguoiLienHeDienThoai String?    @db.VarChar(50)
  lienLacSoNhaDuong    String?    @db.VarChar(255)
  lienLacPhuongXa      String?    @db.VarChar(50)
  thuongTruSoNhaDuong  String?    @db.VarChar(255)
  thuongTruPhuongXa    String?    @db.VarChar(50)
  thuongTruQuocGia     String?    @db.VarChar(50)
  doanNgayVao          DateTime?
  dangNgayVao          DateTime?
  bhytSoThe            String?    @db.VarChar(255)
  bhtnSoThe            String?    @db.VarChar(255)
  hinhThePath          String?    @db.VarChar(500)
  createdAt            DateTime   @default(now())
  updatedAt            DateTime   @updatedAt
  nhDaoTaos            NhDaoTao[]

  @@map("nguoi_hoc")
}

/// Bảng Người học - Đào tạo
model NhDaoTao {
  id                  Int              @id @default(autoincrement())
  cccdSo              String?          @db.VarChar(20)
  maNguoiHoc          String?          @db.VarChar(20)
  trinhDoDaoTao       String?          @db.VarChar(50)
  emailTruong         String?          @db.VarChar(50)
  maTuyenSinh         String?          @db.VarChar(50)
  tsMaNganh           String?          @db.VarChar(20)
  doiTuongUuTien      String?          @db.VarChar(50)
  khuVucTuyenSinh     String?          @db.VarChar(50)
  truongThpt          String?          @db.VarChar(50)
  trungTuyenSoQd      String?          @db.VarChar(50)
  trungTuyenNgayQd    DateTime?
  trungTuyenToHopMon  String?          @db.VarChar(50)
  diemMon1            String?          @db.VarChar(10)
  diemMon2            String?          @db.VarChar(10)
  diemMon3            String?          @db.VarChar(10)
  diemUuTien          String?          @db.VarChar(10)
  tongDiemXetTuyen    String?          @db.VarChar(10)
  dtMaCtdt            String?          @db.VarChar(50)
  dtMaNganh           String?          @db.VarChar(50)
  loaiHinhDaoTao      String?          @db.VarChar(50)
  heDaoTao            String?          @db.VarChar(50)
  buoiDaoTao          String?          @db.VarChar(50)
  thangVao            Int?             @db.SmallInt
  namVao              Int?             @db.SmallInt
  namRa               Int?             @db.SmallInt
  maLop               String?          @db.VarChar(50)
  khoa                String?          @db.VarChar(50)
  donViQuanLy         String?          @db.VarChar(50)
  coSoDaoTao          String?          @db.VarChar(50)
  hkGiaHan            Int?             @db.SmallInt
  ngayNhapHoc         DateTime?
  trangThaiNguoiHoc   String?          @db.VarChar(50)
  ngayChuyenTrangThai DateTime?
  qlctdtCtdt          String?          @db.VarChar(255)
  nguoiHocId          Int?
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  nguoiHoc            NguoiHoc?        @relation(fields: [nguoiHocId], references: [id])
  nhVanBangs          NhVanBang[]
  nhViPhamKyLuats     NhViPhamKyLuat[]

  @@index([maNguoiHoc])
  @@map("nh_dao_tao")
}

/// Bảng Người học - Văn bằng
model NhVanBang {
  id           Int       @id @default(autoincrement())
  maNguoiHoc   String?   @db.VarChar(20)
  cccdSo       String?   @db.VarChar(20)
  maCtdt       String?   @db.VarChar(50)
  maNganh      String?   @db.VarChar(50)
  tnSoQd       String?   @db.VarChar(50)
  tnNgayQd     DateTime?
  tnXepLoai    String?   @db.VarChar(50)
  vbNgayCap    DateTime?
  vbSoHieu     String?   @db.VarChar(50)
  vbSoVaoSoGoc String?   @db.VarChar(50)
  nhDaoTaoId   Int?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  nhDaoTao     NhDaoTao? @relation(fields: [nhDaoTaoId], references: [id])

  @@map("nh_van_bang")
}

/// Bảng Người học - Vi phạm kỷ luật
model NhViPhamKyLuat {
  id           Int       @id @default(autoincrement())
  maNguoiHoc   String?   @db.VarChar(20)
  cccdSo       String?   @db.VarChar(20)
  loaiViPham   String?   @db.VarChar(50)
  hinhThucXuLy String?   @db.VarChar(255)
  noiDungLyDo  String?   @db.VarChar(500)
  loaiKyLuat   String?   @db.VarChar(50)
  soQd         String?   @db.VarChar(50)
  ngayQd       DateTime?
  namHocHocKy  Int?      @db.SmallInt
  nhDaoTaoId   Int?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  nhDaoTao     NhDaoTao? @relation(fields: [nhDaoTaoId], references: [id])

  @@map("nh_vi_pham_ky_luat")
}

enum UserRole {
  admin
  reader
  writer
  user
}

model User {
  id           Int       @id @default(autoincrement())
  username     String    @unique @db.VarChar(100)
  email        String    @unique @db.VarChar(150)
  passwordHash String    @map("password_hash")
  fullName     String?   @map("full_name") @db.VarChar(150)
  isActive     Boolean?  @default(true) @map("is_active")
  createdAt    DateTime? @default(now()) @map("created_at") @db.Timestamp(6)
  role         UserRole  @default(reader) @map("role")
  /// Cấu hình quyền truy cập bảng dạng YAML-like:
  /// { "writeScopes": ["^tcns_.*"], "readScopes": ["^dm_.*"] }
  /// admin: null (bypass toàn bộ)
  roleSettings Json?     @map("role_settings")

  @@map("users")
}